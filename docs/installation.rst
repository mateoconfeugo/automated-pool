============
Installation
============

At the command line::

    $ easy_install automated-pool

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv automated-pool
    $ pip install automated-pool